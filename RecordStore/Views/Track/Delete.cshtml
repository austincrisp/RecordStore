@model RecordStore.Models.Track

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>Album</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.TrackTitle)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.TrackTitle)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Length)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Length)
        </dd>

        <dt>
            @Html.Hidden("TrackId", Model.Id, null)
        </dt>

        <dd>
            @Html.Hidden("TrackId", Model.Id, null)
        </dd>

    </dl>

    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-danger" /> |
            @Html.ActionLink("Back to Tracks", "Tracks", "Band", new { id = Model.Album.Id }, null)
        </div>
    }
</div>